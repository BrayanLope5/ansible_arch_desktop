- name: Create a symbolic link from the Applications folder to the binaries folder.
  ansible.builtin.file:
    src: "{{nonroot_home}}/Applications/bin"
    dest: "/usr/local/bin/downloaded"
    state: link
    force: yes
